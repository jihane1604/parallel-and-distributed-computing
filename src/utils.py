# square function 
def square(n = 0):
    """
    Compute the square of the given number.

    Args:
        n (int, optional): The number to square. Defaults to 0.

    Returns:
        int: The square of n.
    """
    return n * n